export interface DesignTokens {
  colors: {
    primary: string;
    secondary: string;
    accent: string;
    background: string;
    text: string;
    muted: string;
  };
  fonts: {
    heading: string;
    body: string;
    accent: string;
  };
  layout: {
    maxWidth: string;
    heroHeight: string;
    sectionSpacing: string;
  };
}
